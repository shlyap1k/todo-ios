# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'todo-ios' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for todo-ios
  pod 'RealmSwift', '~> 10.47.0'
  pod 'RxSwift', '~> 6.8.0'
  pod 'SwiftLint'
  pod 'SwiftFormat'

  target 'todo-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'todo-iosUITests' do
    # Pods for testing
  end

end
